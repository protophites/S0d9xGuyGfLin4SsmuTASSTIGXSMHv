package me.protophite.core.module;


import org.bukkit.configuration.file.YamlConfiguration;

public abstract class ProModule {
/*
    FrontierFile configFile;

    boolean enabled;
    String name;
    DataStructure data;

    ProModule(FrontierFile configFile){
        this.configFile = configFile;
        YamlConfiguration config = configFile.getConfig();


        this.name = file.getFile().getName().replace(".yml", "");
    }

    public void fetchData(DataStructure data){
        structure.
    }
    public String getName(){return name;}
*/
}
