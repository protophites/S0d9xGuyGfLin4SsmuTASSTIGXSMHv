package me.protophite.core.anticheat.modules.cheatdata;

public class CheatData {
/*
    private static final MutableLine violationLine = new MutableLine("§4Violations: §c%amount%");

    //TODO load violations from database.
    private Map<Button, List<Long>> violations = new HashMap<>();
    private Core instance;
    private UUID uuid;


    public CheatData(Player p){
        instance = Core.getInstance();
        Cerberus cerb = instance.getCerberus();

        for(AntiCheat ac : cerb.getAllAntiCheats()){
            Button button = ac.getMainButton();


                TODO: Load violations from data
                TODO: Get dates
                TODO: Set to correct cheat



            violationLine.mutate("0");
            button.setLore(Collections.singletonList(violationLine.getCurrent()));

        }
    }
*/


}
