package me.protophite.core.anticheat.modules.speedmine;

import me.protophite.core.anticheat.modules.AntiCheat;
import org.bukkit.Material;
import org.bukkit.configuration.ConfigurationSection;
import org.bukkit.event.Listener;

import java.util.HashMap;
import java.util.Map;

public class SpeedMine extends AntiCheat {
/*
    private boolean instantBreakCheck;
    private final Map<Material, Long> speedMineMats = new HashMap<>();

    public SpeedMine(ConfigurationSection section) {
        super(section);

        instantBreakCheck = section.getBoolean("Check-Instant-Breaks");

    }
    */
}
